agent_type: chat_agent

components:
  - planner
  - responder
  - executor

features:
  memory: true
  observability: true
  prompt_versioning: true

required_prompts:
  - planner
  - responder

entrypoint:
  orchestration_graph: orchestration/build_graph.py