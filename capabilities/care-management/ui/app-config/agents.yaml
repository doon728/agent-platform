# Application configuration
# This file is populated by Agent Factory when an app repo is generated.

industry: healthcare

app_name: app-template

tool_gateway:
  name: healthcare-tool-gateway
  mode: external_shared
  # Endpoint will be injected by Agent Factory or environment config
  endpoint: TBD

# List of agents used by this application
# Agent Factory will populate this section
agents: []