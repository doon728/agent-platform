# platform-core: shared Python library imported by all agent runtimes.
# Provides schema validation (domain.yaml, agent.yaml) and plugin registry
# for adapters (memory backend, HITL adapter, RAG pattern, reasoning strategy).
